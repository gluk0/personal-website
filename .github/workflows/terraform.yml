name: 'Terraform Deploy'

on:
  push:
    # For dummy commits to main to trigger builds. 
    branches: [ "main" ]
    paths:
      - 'infrastructure/**'
  pull_request:
    # on merge to main auto kick off tf pipeline if dir modified == infrastructure. 
    branches: [ "main" ]
    paths:
      - 'infrastructure/**'


env:
  PROJECT_ID: rich-clarke-dev  
  TF_STATE_BUCKET: rich-clarke-2421-statebucket

jobs:
  terraform:
    name: 'Terraform'
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./infrastructure

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: 'Authenticate to Google Cloud'
      uses: 'google-github-actions/auth@v1'
      with:
        credentials_json: '${{ secrets.GCP_SA_KEY }}'

    - name: 'Set up Cloud SDK'
      uses: 'google-github-actions/setup-gcloud@v1'

    - name: 'Setup Terraform'
      uses: hashicorp/setup-terraform@v2
      with:
        terraform_version: '1.5.0'

    - name: 'Terraform Format'
      run: terraform fmt -check
      continue-on-error: true

    - name: 'Terraform Init'
      run: |
        terraform init \
          -backend-config="bucket=$TF_STATE_BUCKET" \
          -backend-config="prefix=portfolio/state"

    - name: 'Terraform Plan'
      run: terraform plan -no-color
      continue-on-error: true

    - name: 'Terraform Apply'
      run: terraform apply -auto-approve 
